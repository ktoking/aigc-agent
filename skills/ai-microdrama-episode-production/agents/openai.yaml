interface:
  display_name: "AI 短剧剧集生产"
  short_description: "生成剧集包并整理校验首尾帧资产"
  default_prompt: "使用 $ai-microdrama-episode-production，基于 stories/<story-id> 生成或整理一集结构化短剧包，并校验首尾帧图片。"

policy:
  allow_implicit_invocation: true
