# 本剧生产工作流

1. 固定真人角色定妆、表情、服装与可复用场景层。
2. 为每集写 60 秒四段剧情，并先确定段尾钩子。
3. 先生成旁白/对白音轨，按音频时长定字幕和镜头。
4. 每段先按 `shot-list.md` 生成 3-4 张独立真人镜头图；每图停留 3-5 秒，完成一次明确的信息或情绪变化。
5. 默认用 HyperFrames 做镜头切换、慢推、背景视差、特效、字幕与混音；`first-frame` 和 `last-frame` 只负责段落连续性。
6. 只有怪物扑击、压跪等无法用静帧转场表达的短动作，才单独进入视频 API 测试；不整段重烧。
