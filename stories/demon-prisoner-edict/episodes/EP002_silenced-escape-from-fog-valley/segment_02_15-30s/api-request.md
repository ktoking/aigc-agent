# API 交接
- 模型：`doubao-seedance-2-0-mini-260615`；480p、16:9、12–15 秒、`generate_audio: false`。
- 图像：`first-frame.png` / `shot-02.png` / `last-frame.png` 待生成；骨针、禁声符锁、人物锚点必须作为参考。
- 状态：未提交；先锁定“符锁被偏转而非消失”的动作因果。
